Implement iOS native pull-to-refresh functionality using UIRefreshControl inside the app’s WebView. When the user performs a swipe-down gesture, the current page should reload smoothly without visual glitches or blank screens.


The app currently renders in full screen and overlaps with the iOS status bar (top) and home indicator/navigation area (bottom). Update the layout and make appropriate space at the top and bottom for status bar and home indicator.  

When a user taps on a link inside the WebView, a white screen briefly flashes before the next page loads & it feels like lagging. Eliminate this flicker and ensure a smooth, instant transition between pages. Optimize WebView loading behavior to prevent blank rendering states.




Handle External Links Inside App (Except Own Domain)

Modify link handling logic so that:

All external links open inside the app using an in-app browser (e.g., SFSafariViewController or equivalent).
Links belonging to https://findjobsinfinland.fi/ should continue to open within the main WebView.
Prevent opening links in the device’s default browser unless explicitly required.



put a back button in the top, make like put a different url for ios like https://findjobsinfinland.fi/?ios and if there is ?ios in the url then display the back button otherwise hide it 

take me back in 404 navigates back to the previous page



Convert my existing Capacitor iOS app into a complete native Swift iOS application without changing the current user experience or removing any functionality.

Main requirement:
Keep the app working exactly like the current version, but remove Capacitor dependency and rebuild it using native Swift, WKWebView, and iOS APIs.

Important:
Do not remove any existing feature from the current Capacitor app. The goal is only to replace Capacitor with native Swift while keeping the same app behavior.