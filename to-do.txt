I want to send app notification along with web like currently we have push notification in web so for our android and ios app also we should be able to send push notification and also there is a problem that our app just loads our website into it so it doesn't know who is logged in or not so we should be able to send push notification to the user based on their login status, also we should be able to send push notification to the user based on their preferences, so im thinking to send a message to app after user logs in or sign up inside the app using the following logic : 

Website login inside app
    ↓
Website gets user details and stores it in firebase
    ↓
Website sends message to app that following user with following uid has been registered and app receives the details uid, email, fullName of the user and stores it in app
    ↓
app asks for notification permission with user and gets its own FCM token 
    ↓
app sends fcmToken accroding to the user's uid, and also logged in platform name to firebase
    ↓
firebase sends notification to the app also

change app open color to this 




there is a bug somewhere in our code where the fcm tokens are not being stored properly or whatever because the user is not receiving the push notification on the device even after allowing notification, so check all of our files and fix it 

remmove invalid fcm tokens


change the footer in all the pages 

modify landing page




images loading delay

app lagging






now remove all the fcm token storing logic of user if allowed notification like here in our code we have if user clicked yes in a popup than it ask for notification permission and stores the fcm token to so remove that logic of asking notification and storing fcm tokens only and also remove all other fcm token storing logic from our code except one place in android app where its stored in appdevices


footer unresponsive on medium small device : means as you can see the support and quick links are being unresponsive